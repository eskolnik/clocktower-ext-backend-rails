<div>
  <h1 class="font-bold text-2xl">Broadcasters#show</h1>
  <table class="border-collapse border border-gray-500">
    <tr>
        <th class="border border-gray-500">Channel ID</th>
        <th class="border border-gray-500">Secret Key</th>
    </tr>

    <tr>
        <td class="border border-gray-500"><%= @broadcaster.channel_id %></td>
        <td class="border border-gray-500"><%= @broadcaster.secret_key %></td>
    </tr>
  </table>
</div>
